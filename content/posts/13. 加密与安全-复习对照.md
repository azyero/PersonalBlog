---
title: 13. 加密与安全 - 复习对照
date: 2026-05-11
summary: 加密与安全的目标，是理解常见安全工具各自解决的问题：编码、摘要、认证、加密、密钥交换、签名和证书不是一回事。
tags: [Java, 技术总结, 加密与安全]
---

# 13. 加密与安全 - 复习对照

> 按 `14. 多线程-总结.md` 的学习笔记方式重写：先给全局结构，再逐主题讲清楚问题、方法、适用场景和易错点。

## 一句话总览

加密与安全的目标，是理解常见安全工具各自解决的问题：编码、摘要、认证、加密、密钥交换、签名和证书不是一回事。

## 本章知识结构

| 模块 | 解决的问题 | 核心关键词 |
| --- | --- | --- |
| 本章总览 | 13.3. BouncyCastle | `编码`、`哈希算法`、`Hmac`、`数字签名`、`数字证书` |
| 编码算法 | URL编码和Base64编码都是编码算法，它们不是加密算法； | `编码`、`包`、`String`、`数组`、`int`、`byte` |
| 哈希算法 | 哈希算法可用于验证数据完整性，具有防篡改检测的功能； | `哈希算法`、`String`、`方法`、`接口`、`数组`、`包`、`hashCode`、`int` |
| BouncyCastle | BouncyCastle是一个开源的第三方算法提供商； | `哈希算法`、`方法`、`JAR`、`包`、`classpath` |
| Hmac算法 | Hmac算法是一种标准的基于密钥的哈希算法，可以配合MD5、SHA-1等哈希算法，计算的摘要长度和原摘要算法长度相同。 | `Hmac`、`哈希算法`、`数组`、`byte`、`new` |
| 对称加密算法 | 对称加密算法使用同一个密钥进行加密和解密，常用算法有DES、AES和IDEA等； | `包`、`AES`、`接口`、`方法` |
| 口令加密算法 | PBE算法通过用户口令和安全的随机salt计算出Key，然后再进行加密； | `AES`、`方法` |
| 密钥交换算法 | DH算法是一种密钥交换协议，通信双方通过不安全的信道协商密钥，然后进行对称加密传输。 | `AES`、`方法` |
| 非对称加密算法 | 非对称加密就是加密和解密使用的不是相同的密钥，只有同一个公钥-私钥对才能正常加解密； | `RSA`、`AES`、`方法`、`数组`、`byte` |
| 签名算法 | 数字签名就是用发送方的私钥对原始数据进行签名，只有用发送方公钥才能通过签名验证。 | `数字签名`、`哈希算法`、`RSA`、`包` |
| 数字证书 | 数字证书就是集合了多种密码学算法，用于实现数据加解密、身份认证、签名等多种功能的一种安全标准； | `数字证书`、`数字签名`、`JDK`、`RSA`、`包`、`AES` |

## 学习主线

这一章可以按“先理解概念，再看 API，再回到场景判断什么时候用”的顺序学习。不要只背方法名；每个知识点都要回答三个问题：它解决什么问题、它靠什么机制解决、它在哪些边界条件下容易出错。

本章反复出现的概念有：`编码`、`哈希算法`、`Hmac`、`数字签名`、`数字证书`、`包`、`String`、`数组`、`方法`、`接口`、`JAR`、`classpath`、`AES`、`RSA`、`JDK`。

代码层面需要熟悉的关键词有：`int`、`byte`、`hashCode`、`class`、`equals`、`new`。

## 1. 本章总览

这一部分覆盖：13. 加密与安全。

它的核心是：13.3. BouncyCastle

- 相关概念：`编码`、`哈希算法`、`Hmac`、`数字签名`、`数字证书`

**重点理解**

- 13.3. BouncyCastle

## 2. 编码算法

这一部分覆盖：13.1. 编码算法。

它的核心是：URL编码和Base64编码都是编码算法，它们不是加密算法；；URL编码的目的是把任意文本数据编码为%前缀表示的文本，便于浏览器和服务器处理；

- 相关概念：`编码`、`包`、`String`、`数组`
- 代码关键词：`int`、`byte`

**重点理解**

- URL编码和Base64编码都是编码算法，它们不是加密算法；
- URL编码的目的是把任意文本数据编码为%前缀表示的文本，便于浏览器和服务器处理；
- Base64编码的目的是把任意二进制数据编码为文本，但编码后数据量会增加1/3。

**复习抓手**

- URL编码
- Base64编码
- 小结

## 3. 哈希算法

这一部分覆盖：13.2. 哈希算法。

它的核心是：哈希算法可用于验证数据完整性，具有防篡改检测的功能；；常用的哈希算法有MD5、SHA-1等；

- 相关概念：`哈希算法`、`String`、`方法`、`接口`、`数组`、`包`
- 代码关键词：`hashCode`、`int`、`class`、`equals`、`byte`

**重点理解**

- 哈希算法可用于验证数据完整性，具有防篡改检测的功能；
- 常用的哈希算法有MD5、SHA-1等；
- 用哈希存储口令时要考虑彩虹表攻击。

**复习抓手**

- 哈希碰撞
- 哈希算法的用途
- SHA-1
- 小结

## 4. BouncyCastle

这一部分覆盖：13.3. BouncyCastle。

它的核心是：BouncyCastle是一个开源的第三方算法提供商；；BouncyCastle提供了很多Java标准库没有提供的哈希算法和加密算法；

- 相关概念：`哈希算法`、`方法`、`JAR`、`包`、`classpath`

**重点理解**

- BouncyCastle是一个开源的第三方算法提供商；
- BouncyCastle提供了很多Java标准库没有提供的哈希算法和加密算法；
- 使用第三方算法前需要通过Security.addProvider()注册。

**复习抓手**

- 小结

## 5. Hmac算法

这一部分覆盖：13.4. Hmac算法。

它的核心是：Hmac算法是一种标准的基于密钥的哈希算法，可以配合MD5、SHA-1等哈希算法，计算的摘要长度和原摘要算法长度相同。

- 相关概念：`Hmac`、`哈希算法`、`数组`
- 代码关键词：`byte`、`new`

**重点理解**

- Hmac算法是一种标准的基于密钥的哈希算法，可以配合MD5、SHA-1等哈希算法，计算的摘要长度和原摘要算法长度相同。

**复习抓手**

- 小结

## 6. 对称加密算法

这一部分覆盖：13.5. 对称加密算法。

它的核心是：对称加密算法使用同一个密钥进行加密和解密，常用算法有DES、AES和IDEA等；；密钥长度由算法设计决定，AES的密钥长度是128/192/256位；

- 相关概念：`包`、`AES`、`接口`、`方法`

**重点理解**

- 对称加密算法使用同一个密钥进行加密和解密，常用算法有DES、AES和IDEA等；
- 密钥长度由算法设计决定，AES的密钥长度是128/192/256位；
- 使用对称加密算法需要指定算法名称、工作模式和填充模式。

**复习抓手**

- 使用AES加密
- 小结

## 7. 口令加密算法

这一部分覆盖：13.6. 口令加密算法。

它的核心是：PBE算法通过用户口令和安全的随机salt计算出Key，然后再进行加密；；Key通过口令和安全的随机salt计算得出，大大提高了安全性；

- 相关概念：`AES`、`方法`

**重点理解**

- PBE算法通过用户口令和安全的随机salt计算出Key，然后再进行加密；
- Key通过口令和安全的随机salt计算得出，大大提高了安全性；
- PBE算法内部使用的仍然是标准对称加密算法（例如AES）。

**复习抓手**

- 小结

## 8. 密钥交换算法

这一部分覆盖：13.7. 密钥交换算法。

它的核心是：DH算法是一种密钥交换协议，通信双方通过不安全的信道协商密钥，然后进行对称加密传输。；DH算法没有解决中间人攻击。

- 相关概念：`AES`、`方法`

**重点理解**

- DH算法是一种密钥交换协议，通信双方通过不安全的信道协商密钥，然后进行对称加密传输。
- DH算法没有解决中间人攻击。

**复习抓手**

- 小结

## 9. 非对称加密算法

这一部分覆盖：13.8. 非对称加密算法。

它的核心是：非对称加密就是加密和解密使用的不是相同的密钥，只有同一个公钥-私钥对才能正常加解密；；只使用非对称加密算法不能防止中间人攻击。

- 相关概念：`RSA`、`AES`、`方法`、`数组`
- 代码关键词：`byte`

**重点理解**

- 非对称加密就是加密和解密使用的不是相同的密钥，只有同一个公钥-私钥对才能正常加解密；
- 只使用非对称加密算法不能防止中间人攻击。

**复习抓手**

- 小结

## 10. 签名算法

这一部分覆盖：13.9. 签名算法。

它的核心是：数字签名就是用发送方的私钥对原始数据进行签名，只有用发送方公钥才能通过签名验证。；数字签名用于：

- 相关概念：`数字签名`、`哈希算法`、`RSA`、`包`

**重点理解**

- 数字签名就是用发送方的私钥对原始数据进行签名，只有用发送方公钥才能通过签名验证。
- 数字签名用于：
- 防止伪造；
- 防止抵赖；
- 检测篡改。
- 常用的数字签名算法包括：MD5withRSA／SHA1withRSA／SHA256withRSA／SHA1withDSA／SHA256withDSA／SHA512withDSA／ECDSA等。

**复习抓手**

- DSA签名
- ECDSA签名
- 小结

## 11. 数字证书

这一部分覆盖：13.10. 数字证书。

它的核心是：数字证书就是集合了多种密码学算法，用于实现数据加解密、身份认证、签名等多种功能的一种安全标准；；数字证书采用链式签名管理，顶级的Root CA证书已内置在操作系统中；

- 相关概念：`数字证书`、`数字签名`、`JDK`、`RSA`、`包`、`AES`

**重点理解**

- 数字证书就是集合了多种密码学算法，用于实现数据加解密、身份认证、签名等多种功能的一种安全标准；
- 数字证书采用链式签名管理，顶级的Root CA证书已内置在操作系统中；
- 数字证书存储的是公钥，可以安全公开，而私钥必须严格保密。

**复习抓手**

- 小结

## 选择指南

| 遇到的问题 | 优先考虑 |
| --- | --- |
| 需要处理“本章总览”相关问题 | `编码`、`哈希算法`、`Hmac`、`数字签名` |
| 需要处理“编码算法”相关问题 | `编码`、`包`、`String`、`数组` |
| 需要处理“哈希算法”相关问题 | `哈希算法`、`String`、`方法`、`接口` |
| 需要处理“BouncyCastle”相关问题 | `哈希算法`、`方法`、`JAR`、`包` |
| 需要处理“Hmac算法”相关问题 | `Hmac`、`哈希算法`、`数组`、`byte` |
| 需要处理“对称加密算法”相关问题 | `包`、`AES`、`接口`、`方法` |
| 需要处理“口令加密算法”相关问题 | `AES`、`方法` |
| 需要处理“密钥交换算法”相关问题 | `AES`、`方法` |
| 需要处理“非对称加密算法”相关问题 | `RSA`、`AES`、`方法`、`数组` |
| 需要处理“签名算法”相关问题 | `数字签名`、`哈希算法`、`RSA`、`包` |

## 易错点清单

- 编码不是加密，只是数据表示方式转换。
- 哈希不可逆，适合摘要和完整性校验，不适合保存明文秘密。
- 口令不能直接哈希，应使用专门口令算法、盐和足够成本。
- 对称加密快但密钥分发困难，非对称加密慢但适合密钥交换和签名。
- 签名保证身份和防篡改，不保证内容保密。

## 推荐复习路线

1. 先读“一句话总览”和“本章知识结构”，确认这一章在整个 Java 学习路线里的位置。
2. 按编号阅读各主题，看到表格时先抓“解决的问题”，再看“关键点”。
3. 回到源文件运行或重写关键示例，尤其是涉及 API、配置、异常、边界条件的部分。
4. 最后只看“易错点清单”，能解释每一条为什么会错，就说明这一章基本吃透。
