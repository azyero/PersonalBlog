---
title: 19. 函数式编程 - 复习对照
date: 2026-05-11
summary: 函数式编程的目标，是把行为当成值传递，让集合处理从“手写循环”升级为 Lambda、方法引用和 Stream 管道。
tags: [Java, 技术总结, 函数式编程]
---

# 19. 函数式编程 - 复习对照

> 按 `14. 多线程-总结.md` 的学习笔记方式重写：先给全局结构，再逐主题讲清楚问题、方法、适用场景和易错点。

## 一句话总览

函数式编程的目标，是把行为当成值传递，让集合处理从“手写循环”升级为 Lambda、方法引用和 Stream 管道。

## 本章知识结构

| 模块 | 解决的问题 | 核心关键词 |
| --- | --- | --- |
| 本章总览 | 理解并掌握 本章总览 的核心概念和用法 | `函数式编程`、`Lambda`、`方法引用`、`Stream`、`方法`、`变量`、`this` |
| Lambda基础 | 单方法接口被称为FunctionalInterface。 | `Lambda`、`函数式编程`、`方法`、`变量`、`编码`、`接口`、`String`、`注解` |
| 方法引用 | FunctionalInterface允许传入： | `方法引用`、`函数式编程`、`Lambda`、`方法`、`接口`、`String`、`继承`、`构造方法` |
| 19.3. 使用Stream | Stream API的特点是： | `Stream`、`函数式编程`、`Lambda`、`InputStream`、`OutputStream`、`序列化`、`List`、`方法` |

## 学习主线

这一章可以按“先理解概念，再看 API，再回到场景判断什么时候用”的顺序学习。不要只背方法名；每个知识点都要回答三个问题：它解决什么问题、它靠什么机制解决、它在哪些边界条件下容易出错。

本章反复出现的概念有：`函数式编程`、`Lambda`、`方法引用`、`Stream`、`方法`、`变量`、`编码`、`接口`、`String`、`注解`、`继承`、`构造方法`、`List`、`泛型`、`InputStream`、`OutputStream`、`序列化`、`数组`、`Set`、`Queue`、`正则表达式`、`基本类型`、`Map`、`包`、`线程`。

代码层面需要熟悉的关键词有：`this`、`equals`、`class`、`return`、`int`、`default`、`static`、`boolean`、`Object`、`String`、`for`、`new`、`byte`、`char`、`Integer`、`super`。

## 1. 本章总览

这一部分覆盖：19. 函数式编程。

- 相关概念：`函数式编程`、`Lambda`、`方法引用`、`Stream`、`方法`、`变量`
- 代码关键词：`this`

## 2. Lambda基础

这一部分覆盖：19.1. Lambda基础。

它的核心是：单方法接口被称为FunctionalInterface。；接收FunctionalInterface作为参数的时候，可以把实例化的匿名类改写为Lambda表达式，能大大简化代码。

- 相关概念：`Lambda`、`函数式编程`、`方法`、`变量`、`编码`、`接口`、`String`、`注解`
- 代码关键词：`equals`、`this`、`class`、`return`、`int`、`default`、`static`、`boolean`、`Object`

**重点理解**

- 单方法接口被称为FunctionalInterface。
- 接收FunctionalInterface作为参数的时候，可以把实例化的匿名类改写为Lambda表达式，能大大简化代码。
- Lambda表达式的参数和返回值均可由编译器自动推断。

**复习抓手**

- Lambda表达式
- FunctionalInterface
- 小结

## 3. 方法引用

这一部分覆盖：19.2. 方法引用。

它的核心是：FunctionalInterface允许传入：；接口的实现类（传统写法，代码较繁琐）；

- 相关概念：`方法引用`、`函数式编程`、`Lambda`、`方法`、`接口`、`String`、`继承`、`构造方法`、`List`、`Stream`、`泛型`
- 代码关键词：`int`、`String`、`this`、`for`、`return`、`new`

**重点理解**

- FunctionalInterface允许传入：
- 接口的实现类（传统写法，代码较繁琐）；
- Lambda表达式（只需列出参数名，由编译器推断类型）；
- 符合方法签名的静态方法；
- 符合方法签名的实例方法（实例类型被看做第一个参数类型）；
- 符合方法签名的构造方法（实例类型被看做返回类型）。
- FunctionalInterface不强制继承关系，不需要方法名称相同，只要求方法参数（类型和数量）与方法返回类型相同，即认为方法签名相同。

**复习抓手**

- 构造方法引用
- 小结

## 4. 19.3. 使用Stream

这一部分覆盖：19.3. 使用Stream、19.3.1. 创建Stream、19.3.2. 使用map、19.3.3. 使用filter、19.3.4. 使用reduce、19.3.5. 输出集合、19.3.6. 其他操作。

它的核心是：Stream API的特点是：；Stream API提供了一套新的流式处理的抽象序列；

| 小节 | 要掌握什么 | 关键点 |
| --- | --- | --- |
| 000 使用Stream | Stream API的特点是： | `Stream`、`函数式编程`、`Lambda`、`InputStream`、`OutputStream`、`序列化`、`List` |
| 19.3.1. 创建Stream | 创建Stream的方法有 ： | `Stream`、`方法`、`数组`、`List`、`Set`、`Queue`、`接口` |
| 19.3.2. 使用map | map()方法用于将一个Stream的每个元素映射成另一个元素并转换成一个新的Stream； | `Stream`、`方法`、`String` |
| 19.3.3. 使用filter | 使用filter()方法可以对一个Stream的每个元素进行测试，通过测试的元素被过滤后生成一个新的Stream。 | `Stream`、`方法` |
| 19.3.4. 使用reduce | reduce()方法将一个Stream的每个元素依次作用于BinaryOperator，并将结果合并。 | `Stream`、`方法`、`Map`、`String`、`for`、`Integer` |
| 19.3.5. 输出集合 | Stream可以输出为集合： | `Stream`、`List`、`String`、`方法`、`Set`、`数组`、`构造方法` |
| 19.3.6. 其他操作 | Stream提供的常用操作有： | `Stream`、`方法`、`Set`、`List`、`线程`、`Integer`、`super` |

**重点理解**

- Stream API的特点是：
- Stream API提供了一套新的流式处理的抽象序列；
- Stream API支持函数式编程和链式操作；
- Stream可以表示无限序列，并且大多数情况下是惰性求值的。
- 创建Stream的方法有 ：
- 通过指定元素、指定数组、指定Collection创建Stream；
- 通过Supplier创建Stream，可以是无限序列；
- 通过其他类的相关方法创建。
- 基本类型的Stream有 IntStream、 LongStream和 DoubleStream。
- map()方法用于将一个Stream的每个元素映射成另一个元素并转换成一个新的Stream；
- 可以将一种元素类型转换成另一种元素类型。
- 使用filter()方法可以对一个Stream的每个元素进行测试，通过测试的元素被过滤后生成一个新的Stream。

**复习抓手**

- 小结
- Stream.of()
- 基于数组或Collection
- 基于Supplier
- 其他方法
- 基本类型
- 输出为List
- 输出为数组
- 输出为Map
- 分组输出
- 排序
- 去重

## 选择指南

| 遇到的问题 | 优先考虑 |
| --- | --- |
| 需要处理“本章总览”相关问题 | `函数式编程`、`Lambda`、`方法引用`、`Stream` |
| 需要处理“Lambda基础”相关问题 | `Lambda`、`函数式编程`、`方法`、`变量` |
| 需要处理“方法引用”相关问题 | `方法引用`、`函数式编程`、`Lambda`、`方法` |
| 需要处理“19.3. 使用Stream”相关问题 | `Stream`、`函数式编程`、`Lambda`、`InputStream` |

## 易错点清单

- Lambda 表达的是函数式接口实例，不是随便一段孤立代码。
- 方法引用是 Lambda 的简写，前提是方法签名匹配。
- Stream 分中间操作和终端操作，没有终端操作不会真正执行。
- Stream 是一次性流水线，不等于集合本身。

## 推荐复习路线

1. 先读“一句话总览”和“本章知识结构”，确认这一章在整个 Java 学习路线里的位置。
2. 按编号阅读各主题，看到表格时先抓“解决的问题”，再看“关键点”。
3. 回到源文件运行或重写关键示例，尤其是涉及 API、配置、异常、边界条件的部分。
4. 最后只看“易错点清单”，能解释每一条为什么会错，就说明这一章基本吃透。
