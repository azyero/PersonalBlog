---
title: 2. 快速入门 - 复习对照
date: 2026-05-11
summary: Java 快速入门的目标，是让你从零跑通一个 Java 程序，并理解后续学习会反复用到的语法地基：变量、类型、运算、条件、循环和数组。
tags: [Java, 技术总结, 快速入门]
---

# 2. 快速入门 - 复习对照

> 按 `14. 多线程-总结.md` 的学习笔记方式重写：先给全局结构，再逐主题讲清楚问题、方法、适用场景和易错点。

## 一句话总览

Java 快速入门的目标，是让你从零跑通一个 Java 程序，并理解后续学习会反复用到的语法地基：变量、类型、运算、条件、循环和数组。

## 本章知识结构

| 模块 | 解决的问题 | 核心关键词 |
| --- | --- | --- |
| 本章总览 | 理解并掌握 本章总览 的核心概念和用法 | `数组`、`变量` |
| Java历史 | 从1996年发布1.0版本开始，到目前为止，最新的Java版本是Java 21： | `字节码`、`JVM`、`Java SE`、`Java EE`、`包`、`Spring`、`JDK`、`JRE` |
| 2.2. 搭建开发环境 | 安装完JDK后，需要设置一个JAVA_HOME的环境变量，它指向JDK的安装目录。在Windows下，它是安装目录，类似... | `JDK`、`JVM`、`包`、`Java SE`、`变量`、`javac`、`JAR`、`/usr/libexec/java_home -v 23` |
| 2.3. Java程序基础 | Java提供了两种变量类型：基本类型和引用类型 | `变量`、`String`、`数组`、`包`、`方法`、`编码`、`class`、`public` |
| 2.4. 流程控制 | 2.4.7. break和continue | `JVM`、`if`、`switch`、`String`、`JDK`、`包`、`IO`、`System.out.println` |
| 2.5. 数组操作 | 遍历数组可以使用`for` 循环，`for` 循环可以访问数组索引，`for` each循环直接迭代每个数组元素，但无法... | `数组`、`包`、`变量`、`JVM`、`for`、`int`、`toString`、`JDK` |

## 学习主线

这一章可以按“先理解概念，再看 API，再回到场景判断什么时候用”的顺序学习。不要只背方法名；每个知识点都要回答三个问题：它解决什么问题、它靠什么机制解决、它在哪些边界条件下容易出错。

本章反复出现的概念有：`数组`、`变量`、`字节码`、`JVM`、`Java SE`、`Java EE`、`包`、`Spring`、`JDK`、`JRE`、`接口`、`javac`、`JAR`、`方法`、`String`、`编码`、`模块`、`Class类`、`基本类型`、`引用类型`、`StringBuilder`、`作用域`、`IO`、`NullPointerException`、`枚举类`、`List`、`Map`。

代码层面需要熟悉的关键词有：`/usr/libexec/java_home -v 23`、`class`、`public`、`main`、`static`、`void`、`String`、`for`、`native`、`default`、`int`、`byte`、`short`、`long`、`float`、`double`、`char`、`boolean`、`true`、`final`、`var`、`null`、`new`、`if`、`switch`、`System.out.println`、`import`、`else`。

## 1. 本章总览

这一部分覆盖：2. 快速入门。

- 相关概念：`数组`、`变量`

## 2. Java历史

这一部分覆盖：2.1. Java历史。

它的核心是：从1996年发布1.0版本开始，到目前为止，最新的Java版本是Java 21：；本教程使用的Java版本是最新版的Java 23。

- 相关概念：`字节码`、`JVM`、`Java SE`、`Java EE`、`包`、`Spring`、`JDK`、`JRE`、`接口`

**重点理解**

- 从1996年发布1.0版本开始，到目前为止，最新的Java版本是Java 21：
- 本教程使用的Java版本是最新版的Java 23。
- 初学者学Java，经常听到JDK、JRE这些名词，它们到底是啥？
- JDK：Java Development Kit
- JRE：Java Runtime Environment
- 简单地说，JRE就是运行Java字节码的虚拟机。但是，如果只有Java源码，要编译成Java字节码，就需要JDK，因为JDK除了包含JRE，还提供了编译器、调试器等开发工具。

**复习抓手**

- Java版本
- 名词解释

## 3. 2.2. 搭建开发环境

这一部分覆盖：2.2. 搭建开发环境、2.2.1. 安装JDK、2.2.2. 第一个Java程序、2.2.3. 使用IDE。

它的核心是：安装完JDK后，需要设置一个JAVA_HOME的环境变量，它指向JDK的安装目录。在Windows下，它是安装目录，类似：；在macOS下，它在~/.bash_profile或 ~/.zprofile里，它是：

| 小节 | 要掌握什么 | 关键点 |
| --- | --- | --- |
| 000 搭建开发环境 | 理解本节的基本概念和示例 | `JDK` |
| 2.2.1. 安装JDK | 安装完JDK后，需要设置一个JAVA_HOME的环境变量，它指向JDK的安装目录。在Windows下，它是安装目录，类似： | `JDK`、`JVM`、`包`、`Java SE`、`变量`、`javac`、`JAR` |
| 2.2.2. 第一个Java程序 | 一个Java源码只能定义一个`public`类型的`class`，并且`class`名称和文件名要完全一致； | `方法`、`String`、`javac`、`字节码`、`class`、`public`、`main` |
| 2.2.3. 使用IDE | Eclipse是由IBM开发并捐赠给开源社区的一个IDE，也是目前应用最广泛的IDE。Eclipse的特点是它本身是Java开发的，并且基于... | `包`、`编码`、`Java SE`、`JRE`、`模块`、`Class类`、`for` |

**重点理解**

- 安装完JDK后，需要设置一个JAVA_HOME的环境变量，它指向JDK的安装目录。在Windows下，它是安装目录，类似：
- 在macOS下，它在~/.bash_profile或 ~/.zprofile里，它是：
- 然后，把JAVA_HOME的 bin 目录附加到系统环境变量PATH上。在Windows下，它长这样：
- 在macOS下，它在~/.bash_profile或 ~/.zprofile里，长这样：
- 把 JAVA_HOME的 bin 目录添加到PATH中是为了在任意文件夹下都可以运行java。打开PowerShell窗口，输入命令java -version，如果一切正常，你会看到如下输出：
- 如果你看到的版本号不是23 ，而是15 、 1.8 之类，说明系统存在多个JDK，且默认JDK不是JDK 23，需要把JDK 23提到PATH前面。
- 一个Java源码只能定义一个`public`类型的`class`，并且`class`名称和文件名要完全一致；
- 使用javac可以将.java源码编译成.`class`字节码；
- 使用java可以运行一个已编译的Java程序，参数是类名。
- Eclipse是由IBM开发并捐赠给开源社区的一个IDE，也是目前应用最广泛的IDE。Eclipse的特点是它本身是Java开发的，并且基于插件结构，即使是对Java开发的支持也是通过插件JDT实现的。
- 除了用于Java开发，Eclipse配合插件也可以作为C/C++开发环境、PHP开发环境、Rust开发环境等。
- IntelliJ Idea是由JetBrains公司开发的一个功能强大的IDE，分为免费版和商用付费版。JetBrains公司的IDE平台也是基于IDE平台+语言插件的模式，支持Python开发环境、Ruby开发环境、PHP开发环境等，这些开发环境也分为免费版和付费版。

**复习抓手**

- 设置环境变量
- JDK
- 如何运行Java程序
- 小结
- Eclipse
- IntelliJ Idea
- NetBeans
- 使用Eclipse
- 安装Eclipse
- 设置Eclipse
- Eclipse IDE结构
- 新建Java项目

## 4. 2.3. Java程序基础

这一部分覆盖：2.3. Java程序基础、2.3.1. Java程序基本结构、2.3.2. 变量和数据类型、2.3.3. 整数运算、2.3.4. 浮点数运算、2.3.5. 布尔运算、2.3.6. 字符和字符串、2.3.7. 数组类型。

它的核心是：Java提供了两种变量类型：基本类型和引用类型；基本类型包括整型，浮点型，布尔型，字符型。

| 小节 | 要掌握什么 | 关键点 |
| --- | --- | --- |
| 000 Java程序基础 | 理解本节的基本概念和示例 | `变量`、`String`、`数组`、`包` |
| 2.3.1. Java程序基本结构 | 理解本节的基本概念和示例 | `方法`、`String`、`数组`、`编码`、`class`、`public`、`main` |
| 2.3.2. 变量和数据类型 | Java提供了两种变量类型：基本类型和引用类型 | `变量`、`基本类型`、`引用类型`、`JVM`、`编码`、`String`、`StringBuilder` |
| 2.3.3. 整数运算 | 整数运算的结果永远是精确的； | `方法`、`int`、`long`、`byte`、`short` |
| 2.3.4. 浮点数运算 | 浮点数常常无法精确表示，并且浮点数的运算结果可能有误差； | `方法`、`float`、`double` |
| 2.3.5. 布尔运算 | 与运算和或运算是短路运算； | `包`、`boolean`、`true` |
| 2.3.6. 字符和字符串 | Java的字符类型`char`是基本类型，字符串类型String是引用类型； | `String`、`基本类型`、`编码`、`引用类型`、`包`、`JVM`、`变量` |
| 2.3.7. 数组类型 | 数组是同一数据类型的集合，数组一旦创建后，大小就不可变； | `数组`、`变量`、`基本类型`、`引用类型`、`String`、`包`、`int` |

**重点理解**

- Java提供了两种变量类型：基本类型和引用类型
- 基本类型包括整型，浮点型，布尔型，字符型。
- 变量可重新赋值，等号是赋值语句，不是数学意义的等号。
- 常量在初始化后不可重新赋值，使用常量便于理解程序意图。
- 整数运算的结果永远是精确的；
- 运算结果会自动提升；
- 可以强制转型，但超出范围的强制转型会得到错误的结果；
- 应该选择合适范围的整型（`int` 或 `long`），没有必要为了节省内存而使用`byte`和 `short`进行整数运算。
- 浮点数常常无法精确表示，并且浮点数的运算结果可能有误差；
- 比较两个浮点数通常比较它们的差的绝对值是否小于一个特定值；
- 整型和浮点型运算时，整型会自动提升为浮点型；
- 可以将浮点型强制转为整型，但超出范围后将始终返回整型的最大值。

**复习抓手**

- 变量
- 基本数据类型
- 整型
- 浮点型
- 布尔类型
- 字符类型
- 引用类型
- 常量
- var关键字
- 变量的作用范围
- 小结
- 溢出

## 5. 2.4. 流程控制

这一部分覆盖：2.4. 流程控制、2.4.1. 输入和输出、2.4.2. if条件判断、2.4.3. switch多重选择、2.4.4. while循环、2.4.5. do while循环、2.4.6. for循环、2.4.7. break和continue。

它的核心是：2.4.7. break和continue；Java提供的输出包括：`System.out.println`() / print() / printf()，其中printf()可以格式化输出；

| 小节 | 要掌握什么 | 关键点 |
| --- | --- | --- |
| 000 流程控制 | 2.4.7. break和continue | `JVM`、`if`、`switch` |
| 2.4.1. 输入和输出 | Java提供的输出包括：`System.out.println`() / print() / printf()，其中printf()可以格式... | `String`、`JDK`、`包`、`IO`、`System.out.println`、`import`、`int` |
| 2.4.2. if条件判断 | `if` ... `else`可以做条件判断，`else`是可选的； | `JVM`、`包`、`方法`、`引用类型`、`变量`、`String`、`NullPointerException` |
| 2.4.3. switch多重选择 | `switch`语句可以做多重选择，然后执行匹配的`case`语句后续代码； | `String`、`枚举类`、`方法`、`变量`、`if`、`switch`、`case` |
| 2.4.4. while循环 | `while`循环先判断循环条件是否满足，再执行循环语句； | `方法`、`变量`、`while`、`true`、`int` |
| 2.4.5. do while循环 | `do` `while`先执行循环，再判断条件； | `while`、`do` |
| 2.4.6. for循环 | `for` 循环通过计数器可以实现复杂循环； | `变量`、`数组`、`包`、`List`、`Map`、`while`、`do` |
| 2.4.7. break和continue | `break`语句可以跳出当前循环； | `while`、`for`、`break`、`continue`、`if` |

**重点理解**

- 2.4.7. break和continue
- Java提供的输出包括：`System.out.println`() / print() / printf()，其中printf()可以格式化输出；
- Java提供Scanner对象来方便输入，读取对应的类型可以使用：scanner.nextLine() /nextInt() / nextDouble() / ...
- `if` ... `else`可以做条件判断，`else`是可选的；
- 不推荐省略花括号{} ；
- 多个`if` ... `else`串联要特别注意判断顺序；
- 要注意`if` 的边界条件；
- 要注意浮点数判断相等不能直接用== 运算符；
- 引用类型判断内容相等要使用`equals`()，注意避免NullPointerException。
- `switch`语句可以做多重选择，然后执行匹配的`case`语句后续代码；
- `switch`的计算结果必须是整型、字符串或枚举类型；
- 注意千万不要漏写`break`，建议打开fall-through警告；

**复习抓手**

- 输出
- 格式化输出
- 输入
- 小结
- else
- 判断引用类型相等
- 编译检查
- switch表达式
- yield
- 灵活使用for循环
- for each循环
- break

## 6. 2.5. 数组操作

这一部分覆盖：2.5. 数组操作、2.5.1. 遍历数组、2.5.2. 数组排序、2.5.3. 多维数组、2.5.4. 命令行参数。

它的核心是：遍历数组可以使用`for` 循环，`for` 循环可以访问数组索引，`for` each循环直接迭代每个数组元素，但无法获取索引；；使用Arrays.`toString`()可以快速获取数组内容。

| 小节 | 要掌握什么 | 关键点 |
| --- | --- | --- |
| 000 数组操作 | 理解本节的基本概念和示例 | `数组`、`包` |
| 2.5.1. 遍历数组 | 遍历数组可以使用`for` 循环，`for` 循环可以访问数组索引，`for` each循环直接迭代每个数组元素，但无法获取索引； | `数组`、`变量`、`JVM`、`for`、`int`、`toString` |
| 2.5.2. 数组排序 | 常用的排序算法有冒泡排序、插入排序和快速排序等； | `数组`、`变量`、`JDK`、`String`、`for` |
| 2.5.3. 多维数组 | 二维数组就是数组的数组，三维数组就是二维数组的数组； | `数组`、`包`、`for` |
| 2.5.4. 命令行参数 | 命令行参数类型是`String`[]数组； | `数组`、`String`、`JVM` |

**重点理解**

- 遍历数组可以使用`for` 循环，`for` 循环可以访问数组索引，`for` each循环直接迭代每个数组元素，但无法获取索引；
- 使用Arrays.`toString`()可以快速获取数组内容。
- 常用的排序算法有冒泡排序、插入排序和快速排序等；
- 冒泡排序使用两层`for` 循环实现排序；
- 交换两个变量的值需要借助一个临时变量；
- 可以直接使用Java标准库提供的Arrays.sort()进行排序；
- 对数组排序会直接修改数组本身。
- 二维数组就是数组的数组，三维数组就是二维数组的数组；
- 多维数组的每个数组元素长度都不要求相同；
- 打印多维数组可以使用Arrays.deepToString()；
- 最常见的多维数组是二维数组，访问二维数组的一个元素使用array[row][col]。
- 命令行参数类型是`String`[]数组；

**复习抓手**

- 打印数组内容
- 小结
- 二维数组
- 三维数组

## 选择指南

| 遇到的问题 | 优先考虑 |
| --- | --- |
| 需要处理“本章总览”相关问题 | `数组`、`变量` |
| 需要处理“Java历史”相关问题 | `字节码`、`JVM`、`Java SE`、`Java EE` |
| 需要处理“2.2. 搭建开发环境”相关问题 | `JDK`、`JVM`、`包`、`Java SE` |
| 需要处理“2.3. Java程序基础”相关问题 | `变量`、`String`、`数组`、`包` |
| 需要处理“2.4. 流程控制”相关问题 | `JVM`、`if`、`switch`、`String` |
| 需要处理“2.5. 数组操作”相关问题 | `数组`、`包`、`变量`、`JVM` |

## 易错点清单

- 不要把 JDK、JRE、JVM 混为一谈：JDK 用来开发，JRE 用来运行，JVM 执行字节码。
- `public class` 名称必须和文件名一致，否则编译会失败。
- 整数除法会截断，浮点数计算会有精度误差，不能直接用等号比较浮点数。
- 数组长度固定，索引从 0 开始，越界会抛异常。
- `break` 跳出循环，`continue` 跳过本轮，二者影响范围不同。

## 推荐复习路线

1. 先读“一句话总览”和“本章知识结构”，确认这一章在整个 Java 学习路线里的位置。
2. 按编号阅读各主题，看到表格时先抓“解决的问题”，再看“关键点”。
3. 回到源文件运行或重写关键示例，尤其是涉及 API、配置、异常、边界条件的部分。
4. 最后只看“易错点清单”，能解释每一条为什么会错，就说明这一章基本吃透。
