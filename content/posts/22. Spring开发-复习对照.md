---
title: 22. Spring开发 - 复习对照
date: 2026-05-11
summary: Spring 开发的目标，是用 IoC 管对象、用 AOP 管横切逻辑，并把数据库、事务、Web、REST、WebSocket 和第三方组件整合进统一应用模型。
tags: [Java, 技术总结, Spring开发]
---

# 22. Spring开发 - 复习对照

> 按 `14. 多线程-总结.md` 的学习笔记方式重写：先给全局结构，再逐主题讲清楚问题、方法、适用场景和易错点。

## 一句话总览

Spring 开发的目标，是用 IoC 管对象、用 AOP 管横切逻辑，并把数据库、事务、Web、REST、WebSocket 和第三方组件整合进统一应用模型。

## 本章知识结构

| 模块 | 解决的问题 | 核心关键词 |
| --- | --- | --- |
| 本章总览 | Spring Framework主要包括几个模块： | `Spring`、`IoC`、`AOP`、`Java EE`、`Spring Boot`、`Spring Cloud`、`包`、`模块` |
| 22.1. IoC容器 | 22.1.3. 使用Annotation配置 | `IoC`、`Spring`、`Spring Bean`、`注解`、`Servlet`、`HTTP`、`事务`、`JavaBean` |
| 22.2. 使用AOP | 如何把切面织入到核心逻辑中？这正是AOP需要解决的问题。换句话说，如果客户端获得了BookService的引用，当调用b... | `AOP`、`Spring`、`注解`、`继承`、`多态`、`方法`、`日志`、`事务` |
| 22.3. 访问数据库 | 22.3.4. 集成Hibernate | `Spring`、`JDBC`、`事务`、`DAO`、`Hibernate`、`JPA`、`MyBatis`、`ORM` |
| 22.4. 开发Web应用 | 22.4.1. 使用Spring MVC | `Spring`、`Spring MVC`、`REST`、`Filter`、`CORS`、`WebSocket`、`Servlet`、`JSP` |
| 22.5. 集成第三方组件 | 22.5.1. 集成JavaMail | `Spring`、`IoC`、`AOP`、`Redis`、`POM`、`Spring Bean`、`方法`、`String` |

## 学习主线

这一章可以按“先理解概念，再看 API，再回到场景判断什么时候用”的顺序学习。不要只背方法名；每个知识点都要回答三个问题：它解决什么问题、它靠什么机制解决、它在哪些边界条件下容易出错。

本章反复出现的概念有：`Spring`、`IoC`、`AOP`、`Java EE`、`Spring Boot`、`Spring Cloud`、`包`、`模块`、`JDBC`、`ORM`、`事务`、`Servlet`、`Spring Bean`、`注解`、`HTTP`、`JavaBean`、`方法`、`依赖注入`、`XML`、`构造方法`、`接口`、`Maven`、`反射`、`String`、`classpath`、`继承`、`Class类`、`List`。

代码层面需要熟悉的关键词有：`new`、`boolean`、`int`、`main`、`native`、`true`、`Object`、`public`、`class`、`final`、`null`、`protected`、`super`、`try`、`finally`、`catch`、`String`、`Long`、`id`、`email`、`long`、`Integer`、`package`、`static`、`void`。

## 1. 本章总览

这一部分覆盖：22. Spring开发。

它的核心是：Spring Framework主要包括几个模块：；支持JDBC和ORM的数据访问模块；

- 相关概念：`Spring`、`IoC`、`AOP`、`Java EE`、`Spring Boot`、`Spring Cloud`、`包`、`模块`、`JDBC`、`ORM`、`事务`、`Servlet`

**重点理解**

- Spring Framework主要包括几个模块：
- 支持JDBC和ORM的数据访问模块；
- 支持基于Servlet的MVC开发；
- 支持基于Reactive的Web开发；
- 以及集成JMS、JavaMail、JMX、缓存等其他模块。
- 我们会依次介绍Spring Framework的主要功能。

**复习抓手**

- Spring Framework

## 2. 22.1. IoC容器

这一部分覆盖：22.1. IoC容器、22.1.1. IoC原理、22.1.2. 装配Bean、22.1.3. 使用Annotation配置、22.1.4. 定制Bean、22.1.5. 使用Resource、22.1.6. 注入配置、22.1.7. 使用条件装配。

它的核心是：22.1.3. 使用Annotation配置；22.1.5. 使用Resource

| 小节 | 要掌握什么 | 关键点 |
| --- | --- | --- |
| 000 IoC容器 | 22.1.3. 使用Annotation配置 | `IoC`、`Spring`、`Spring Bean`、`注解`、`Servlet`、`HTTP`、`事务` |
| 22.1.1. IoC原理 | 我们从上面的代码可以看到，依赖注入可以通过set()方法实现。但依赖注入也可以通过构造方法实现。 | `IoC`、`Spring`、`方法`、`依赖注入`、`XML`、`JavaBean`、`Spring Bean` |
| 22.1.2. 装配Bean | Spring的IoC容器接口是ApplicationContext，并提供了多种实现类； | `Spring Bean`、`IoC`、`Spring`、`Maven`、`XML`、`反射`、`String` |
| 22.1.3. 使用Annotation配置 | 使用Annotation可以大幅简化配置，每个Bean通过@Component和 @Autowired注入； | `注解`、`IoC`、`Spring`、`XML`、`Spring Bean`、`方法`、`构造方法` |
| 22.1.4. 定制Bean | Spring默认使用Singleton创建Bean，也可指定Scope为Prototype； | `Spring Bean`、`IoC`、`Spring`、`Class类`、`List`、`接口`、`变量` |
| 22.1.5. 使用Resource | Spring提供了Resource类便于注入资源文件。 | `IoC`、`Spring`、`InputStream`、`String`、`classpath`、`Maven`、`int` |
| 22.1.6. 注入配置 | Spring容器可以通过@PropertySource自动读取配置，并以@Value("${key}")的形式注入； | `IoC`、`方法`、`classpath`、`Spring`、`注解`、`String`、`JavaBean` |
| 22.1.7. 使用条件装配 | Spring允许通过@Profile配置不同的Bean； | `IoC`、`Spring`、`Profile`、`Spring Bean`、`注解`、`JVM`、`变量` |

**重点理解**

- 22.1.3. 使用Annotation配置
- 22.1.5. 使用Resource
- 我们从上面的代码可以看到，依赖注入可以通过set()方法实现。但依赖注入也可以通过构造方法实现。
- 很多Java类都具有带参数的构造方法，如果我们把BookService改造为通过构造方法注入，那么实现代码如下：
- Spring的IoC容器同时支持属性注入和构造方法注入，并允许混合使用。
- 在设计上，Spring的IoC容器是一个高度可扩展的无侵入容器。所谓无侵入，是指应用程序的组件无需实现Spring的特定接口，或者说，组件根本不知道自己在Spring的容器中运行。这种无侵入的设
- 1. 应用程序组件既可以在Spring的IoC容器中运行，也可以自己编写代码自行组装配置；
- 2. 测试的时候并不依赖Spring容器，可单独进行测试，大大提高了开发效率。
- Spring的IoC容器接口是ApplicationContext，并提供了多种实现类；
- 通过XML配置文件创建IoC容器时，使用ClassPathXmlApplicationContext；
- 持有IoC容器后，通过getBean()方法获取Bean的引用。
- 使用Annotation可以大幅简化配置，每个Bean通过@Component和 @Autowired注入；

**复习抓手**

- 依赖注入方式
- 无侵入容器
- ApplicationContext
- 小结
- 思考
- Scope
- 注入List
- 可选注入
- 创建第三方Bean
- 初始化和销毁
- 使用别名
- 使用FactoryBean

## 3. 22.2. 使用AOP

这一部分覆盖：22.2. 使用AOP、22.2.1. 装配AOP、22.2.2. 使用注解装配AOP、22.2.3. AOP避坑指南。

它的核心是：如何把切面织入到核心逻辑中？这正是AOP需要解决的问题。换句话说，如果客户端获得了BookService的引用，当调用bookService.createBook()时，如何对调用方法进行拦截，并在拦截前后进行安全检查、日志、事务等处理，就相当于完成了所有业务功能。；在Java平台上，对于AOP的织入，有3种方式：

| 小节 | 要掌握什么 | 关键点 |
| --- | --- | --- |
| 000 使用AOP | 如何把切面织入到核心逻辑中？这正是AOP需要解决的问题。换句话说，如果客户端获得了BookService的引用，当调用bookService... | `AOP`、`Spring`、`注解`、`继承`、`多态`、`方法`、`日志` |
| 22.2.1. 装配AOP | 在Spring容器中使用AOP非常简单，只需要定义执行方法，并用AspectJ的注解标注应该在何处触发并执行。 | `AOP`、`接口`、`Spring`、`方法`、`日志`、`Maven`、`String` |
| 22.2.2. 使用注解装配AOP | 使用注解实现AOP需要先定义注解，然后使用@Around("@annotation(name)")实现装配； | `注解`、`AOP`、`包`、`Spring Bean`、`方法`、`Spring`、`事务` |
| 22.2.3. AOP避坑指南 | 由于Spring通过CGLIB实现代理类，我们要避免直接访问Bean的字段，以及由`final`方法带来的“未代理”问题。 | `AOP`、`注解`、`Spring`、`方法`、`Spring Bean`、`NullPointerException`、`变量` |

**重点理解**

- 如何把切面织入到核心逻辑中？这正是AOP需要解决的问题。换句话说，如果客户端获得了BookService的引用，当调用bookService.createBook()时，如何对调用方法进行拦截，并在拦截前后进行安全检查、日志、事务等处理，就相当于完成了所有业务功能。
- 在Java平台上，对于AOP的织入，有3种方式：
- 1. 编译期：在编译时，由编译器把切面调用编译进字节码，这种方式需要定义新的关键字并扩展编译器，AspectJ就扩展了Java编译器，使用关键字aspect来实现织入；
- 2. 类加载器：在目标类被装载到JVM时，通过一个特殊的类加载器，对目标类的字节码重新“增强”；
- 3. 运行期：目标对象和切面都是普通Java类，通过JVM的动态代理功能或者第三方库实现运行期动态织入。
- 最简单的方式是第三种，Spring的AOP实现就是基于JVM的动态代理。由于JVM的动态代理要求必须实现接口，如果一个普通类没有业务接口，就需要通过CGLIB或者Javassist这些第三方库实现。
- 在Spring容器中使用AOP非常简单，只需要定义执行方法，并用AspectJ的注解标注应该在何处触发并执行。
- Spring通过CGLIB动态创建子类等方式来实现AOP代理模式，大大简化了代码。
- 使用注解实现AOP需要先定义注解，然后使用@Around("@annotation(name)")实现装配；
- 使用注解既简单，又能明确标识AOP装配，是使用AOP推荐的方式。
- 由于Spring通过CGLIB实现代理类，我们要避免直接访问Bean的字段，以及由`final`方法带来的“未代理”问题。
- 遇到CglibAopProxy的相关日志，务必要仔细检查，防止因为AOP出现NPE异常。

**复习抓手**

- AOP原理
- 拦截器类型
- 小结
- 怎么肥四？
- 思考

## 4. 22.3. 访问数据库

这一部分覆盖：22.3. 访问数据库、22.3.1. 使用JDBC、22.3.2. 使用声明式事务、22.3.3. 使用DAO、22.3.4. 集成Hibernate、22.3.5. 集成JPA、22.3.6. 集成MyBatis、22.3.7. 设计ORM。

它的核心是：22.3.4. 集成Hibernate；Spring提供了JdbcTemplate来简化JDBC操作；

| 小节 | 要掌握什么 | 关键点 |
| --- | --- | --- |
| 000 访问数据库 | 22.3.4. 集成Hibernate | `Spring`、`JDBC`、`事务`、`DAO`、`Hibernate`、`JPA`、`MyBatis` |
| 22.3.1. 使用JDBC | Spring提供了JdbcTemplate来简化JDBC操作； | `JDBC`、`连接池`、`方法`、`SQL`、`事务`、`Spring`、`IoC` |
| 22.3.2. 使用声明式事务 | Spring提供的声明式事务极大地方便了在数据库中使用事务，正确使用声明式事务的关键在于确定好事务边界，理解事务传播级别。 | `事务`、`Spring`、`JDBC`、`接口`、`Spring Bean`、`方法`、`AOP` |
| 22.3.3. 使用DAO | Spring提供了JdbcDaoSupport来便于我们实现DAO模式； | `DAO`、`Spring`、`XML`、`注解`、`泛型`、`方法` |
| 22.3.4. 集成Hibernate | 在Spring中集成Hibernate需要配置的Bean如下： | `Hibernate`、`方法`、`List`、`String`、`Spring Bean`、`ORM`、`Spring` |
| 22.3.5. 集成JPA | 在Spring中集成JPA要选择一个实现，可以选择Hibernate或EclipseLink； | `JPA`、`Spring`、`Hibernate`、`ORM`、`Servlet`、`接口`、`JDBC` |
| 22.3.6. 集成MyBatis | MyBatis是一个半自动化的ORM框架，需要手写SQL语句，没有自动加载一对多或多对一关系的功能。 | `MyBatis`、`Hibernate`、`JPA`、`ORM`、`Spring Bean`、`注解`、`事务` |
| 22.3.7. 设计ORM | ORM框架就是自动映射数据库表结构到JavaBean的工具，设计并实现一个简单高效的ORM框架并不困难。 | `ORM`、`JDBC`、`JavaBean`、`Hibernate`、`JPA`、`MyBatis`、`SQL` |

**重点理解**

- 22.3.4. 集成Hibernate
- Spring提供了JdbcTemplate来简化JDBC操作；
- 使用JdbcTemplate时，根据需要优先选择高级方法；
- 任何JDBC操作都可以使用保底的execute(ConnectionCallback)方法。
- Spring提供的声明式事务极大地方便了在数据库中使用事务，正确使用声明式事务的关键在于确定好事务边界，理解事务传播级别。
- Spring提供了JdbcDaoSupport来便于我们实现DAO模式；
- 可以基于泛型实现更通用、更简洁的DAO模式。
- 在Spring中集成Hibernate需要配置的Bean如下：
- DataSource；
- LocalSessionFactory；
- HibernateTransactionManager。
- 推荐使用Annotation配置所有的Entity Bean。

**复习抓手**

- JdbcTemplate用法
- 小结
- 回滚事务
- 事务边界
- 事务传播
- Insert操作
- Delete操作
- Update操作
- 使用HQL查询
- XML配置
- 设计ORM接口
- 实现ORM

## 5. 22.4. 开发Web应用

这一部分覆盖：22.4. 开发Web应用、22.4.1. 使用Spring MVC、22.4.2. 使用REST、22.4.3. 集成Filter、22.4.4. 使用Interceptor、22.4.5. 处理CORS、22.4.6. 国际化、22.4.7. 异步处理。

它的核心是：22.4.1. 使用Spring MVC；22.4.4. 使用Interceptor

| 小节 | 要掌握什么 | 关键点 |
| --- | --- | --- |
| 000 开发Web应用 | 22.4.1. 使用Spring MVC | `Spring`、`Spring MVC`、`REST`、`Filter`、`CORS`、`WebSocket`、`Servlet` |
| 22.4.1. 使用Spring MVC | 使用Spring MVC时，整个Web应用程序按如下顺序启动： | `Spring MVC`、`Servlet`、`HTTP`、`JSP`、`Filter`、`MVC`、`接口` |
| 22.4.2. 使用REST | 使用@RestController可以方便地编写REST服务，Spring默认使用JSON作为输入和输出。 | `REST`、`Spring MVC`、`MVC`、`IO`、`JSON`、`Maven`、`Jackson` |
| 22.4.3. 集成Filter | 当一个Filter作为Spring容器管理的Bean存在时，可以通过DelegatingFilterProxy间接地引用它并使其生效。 | `Filter`、`Spring MVC`、`Servlet`、`编码`、`Spring`、`IoC`、`HTTP` |
| 22.4.4. 使用Interceptor | Spring MVC提供了Interceptor组件来拦截Controller方法，使用时要注意Interceptor的作用范围。 | `Filter`、`Servlet`、`Spring MVC`、`方法`、`AOP`、`Spring`、`Spring Bean` |
| 22.4.5. 处理CORS | CORS可以控制指定域的页面JavaScript能否访问API。 | `CORS`、`REST`、`Spring`、`方法`、`数组`、`注解`、`Filter` |
| 22.4.6. 国际化 | 多语言支持需要从HTTP请求中解析用户的Locale，然后针对不同Locale显示不同的语言； | `Spring MVC`、`HTTP`、`String`、`classpath`、`Spring`、`接口`、`数组` |
| 22.4.7. 异步处理 | 在Spring MVC中异步处理请求需要正确配置web.xml，并返回Callable或 DeferredResult对象。 | `Servlet`、`线程`、`IO`、`线程池`、`Spring`、`JDBC`、`事务` |
| 22.4.8. 使用WebSocket | 在Servlet中使用WebSocket需要3.1及以上版本； | `WebSocket`、`HTTP`、`JSON`、`Servlet`、`Spring MVC`、`POM`、`Spring` |

**重点理解**

- 22.4.1. 使用Spring MVC
- 22.4.4. 使用Interceptor
- 22.4.8. 使用WebSocket
- 使用Spring MVC时，整个Web应用程序按如下顺序启动：
- 1. 启动Tomcat服务器；
- 2. Tomcat读取web.xml并初始化DispatcherServlet；
- 3. DispatcherServlet创建IoC容器并自动注册到ServletContext中。
- 启动后，浏览器发出的HTTP请求全部由DispatcherServlet接收，并根据配置转发到指定Controller的指定方法处理。
- 使用@RestController可以方便地编写REST服务，Spring默认使用JSON作为输入和输出。
- 要控制序列化和反序列化，可以使用Jackson提供的@JsonIgnore和 @JsonProperty注解。
- 当一个Filter作为Spring容器管理的Bean存在时，可以通过DelegatingFilterProxy间接地引用它并使其生效。
- Spring MVC提供了Interceptor组件来拦截Controller方法，使用时要注意Interceptor的作用范围。

**复习抓手**

- 配置Spring MVC
- 编写Controller
- 小结
- 处理异常
- 使用@CrossOrigin
- 使用CorsRegistry
- 使用CorsFilter
- 测试
- 获取Locale
- 提取资源文件
- 创建MessageSource
- 实现多语言

## 6. 22.5. 集成第三方组件

这一部分覆盖：22.5. 集成第三方组件、22.5.1. 集成JavaMail、22.5.2. 集成JMS、22.5.3. 使用Scheduler、22.5.4. 集成JMX。

它的核心是：22.5.1. 集成JavaMail；22.5.3. 使用Scheduler

| 小节 | 要掌握什么 | 关键点 |
| --- | --- | --- |
| 000 集成第三方组件 | 22.5.1. 集成JavaMail | `Spring`、`IoC`、`AOP`、`Redis` |
| 22.5.1. 集成JavaMail | Spring可以集成JavaMail，通过简单的封装，能简化邮件发送代码。其核心是定义一个JavaMailSender的Bean，然后调用其... | `Spring`、`POM`、`Spring Bean`、`方法`、`String`、`MVC`、`线程` |
| 22.5.2. 集成JMS | JMS是Java消息服务，可以通过JMS服务器实现消息的异步处理。 | `接口`、`IO`、`变量`、`包`、`日志`、`Queue`、`JDBC` |
| 22.5.3. 使用Scheduler | Spring内置定时任务和Cron任务的支持，编写调度任务十分方便。 | `线程池`、`Spring`、`注解`、`Spring Bean`、`方法`、`日志`、`String` |
| 22.5.4. 集成JMX | 在Spring中使用JMX需要： | `Spring`、`接口`、`线程`、`JVM`、`Spring Bean`、`HTTP`、`注解` |

**重点理解**

- 22.5.1. 集成JavaMail
- 22.5.3. 使用Scheduler
- Spring可以集成JavaMail，通过简单的封装，能简化邮件发送代码。其核心是定义一个JavaMailSender的Bean，然后调用其send()方法。
- JMS是Java消息服务，可以通过JMS服务器实现消息的异步处理。
- 消息服务主要解决Producer和Consumer生产和处理速度不匹配的问题。
- Spring内置定时任务和Cron任务的支持，编写调度任务十分方便。
- 在Spring中使用JMX需要：
- 通过@EnableMBeanExport启用自动注册MBean；
- 编写MBean并实现管理属性和管理操作。

**复习抓手**

- 小结
- 使用Cron任务
- 集成Quartz

## 选择指南

| 遇到的问题 | 优先考虑 |
| --- | --- |
| 需要处理“本章总览”相关问题 | `Spring`、`IoC`、`AOP`、`Java EE` |
| 需要处理“22.1. IoC容器”相关问题 | `IoC`、`Spring`、`Spring Bean`、`注解` |
| 需要处理“22.2. 使用AOP”相关问题 | `AOP`、`Spring`、`注解`、`继承` |
| 需要处理“22.3. 访问数据库”相关问题 | `Spring`、`JDBC`、`事务`、`DAO` |
| 需要处理“22.4. 开发Web应用”相关问题 | `Spring`、`Spring MVC`、`REST`、`Filter` |
| 需要处理“22.5. 集成第三方组件”相关问题 | `Spring`、`IoC`、`AOP`、`Redis` |

## 易错点清单

- IoC 负责对象创建和依赖关系，核心是把控制权交给容器。
- AOP 适合日志、事务、权限等横切逻辑，不适合替代业务建模。
- 声明式事务依赖代理机制，要注意方法调用边界。
- Spring MVC 要区分 Controller、Service、DAO 的职责。
- 集成第三方组件时先看 Bean 生命周期和配置入口。

## 推荐复习路线

1. 先读“一句话总览”和“本章知识结构”，确认这一章在整个 Java 学习路线里的位置。
2. 按编号阅读各主题，看到表格时先抓“解决的问题”，再看“关键点”。
3. 回到源文件运行或重写关键示例，尤其是涉及 API、配置、异常、边界条件的部分。
4. 最后只看“易错点清单”，能解释每一条为什么会错，就说明这一章基本吃透。
