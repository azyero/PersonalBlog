---
title: 24. Spring Cloud开发 - 复习对照
date: 2026-05-11
summary: Spring Cloud 项目章节的目标，是用完整交易系统把微服务拆分、交易引擎、API、行情、推送和 UI 串成一套项目级实践。
tags: [Java, 技术总结, Spring Cloud开发]
---

# 24. Spring Cloud开发 - 复习对照

> 按 `14. 多线程-总结.md` 的学习笔记方式重写：先给全局结构，再逐主题讲清楚问题、方法、适用场景和易错点。

## 一句话总览

Spring Cloud 项目章节的目标，是用完整交易系统把微服务拆分、交易引擎、API、行情、推送和 UI 串成一套项目级实践。

## 本章知识结构

| 模块 | 解决的问题 | 核心关键词 |
| --- | --- | --- |
| 本章总览 | 理解并掌握 本章总览 的核心概念和用法 | `Spring Cloud`、`Spring`、`IoC`、`AOP`、`JDBC`、`ORM`、`MVC`、`Spring Boot` |
| 项目架构设计 | 对一个系统来说，建立一个简单可靠的模型，不但能大大简化系统的设计，而且能以较少的代码实现一个稳定运行的系统，最大限度地减... | `Spring Cloud`、`Kafka`、`Redis`、`模块`、`WebSocket` |
| 搭建项目框架 | 我们以Spring Boot为基础，并通过Maven的模块化配置搭建了项目的基本结构，依赖的基础组件通过Docker D... | `Spring Cloud`、`Maven`、`模块`、`依赖管理`、`POM`、`Spring Boot`、`JDK`、`Redis` |
| 24.3. 设计交易引擎 | 本节我们讨论并实现了一个基于内存的高性能的用户资产系统，其核心只有一个tryTransfer()转账方法，业务逻辑非常简... | `Spring Cloud`、`包`、`事务`、`Map`、`枚举类`、`方法`、`线程`、`JDK` |
| 设计定序系统 | 定序系统负责给每个事件一个唯一递增序列号。通过引用前一个事件的序列号，可以构造一个能自动检测连续性的事件流。 | `Spring Cloud`、`包`、`Spring`、`事务` |
| 设计API系统 | API系统负责认证用户身份，并提供一个唯一的交易入口。 | `Spring Cloud`、`Filter`、`HTTP`、`JSON`、`String`、`序列化`、`Redis`、`Spring` |
| 设计行情系统 | 行情系统是典型的少量写、大量读的模式，非常适合缓存。通过编写Lua脚本可使得更新Redis更加简单。 | `Spring Cloud`、`包`、`Redis`、`List`、`JSON`、`WebSocket` |
| 设计推送系统 | 要高效处理大量WebSocket连接，我们选择基于Netty的Vert.x框架，可以通过少量代码配合Redis实现推送。 | `Spring Cloud`、`包`、`WebSocket`、`JSON`、`Servlet`、`线程池`、`接口`、`Spring` |
| 编写UI | UI系统是标准的Web系统，除了注册、登录外，主要交易功能均由页面JavaScript实现。UI系统本身不是交易入口，它... | `Spring Cloud`、`MVC`、`main` |

## 学习主线

这一章可以按“先理解概念，再看 API，再回到场景判断什么时候用”的顺序学习。不要只背方法名；每个知识点都要回答三个问题：它解决什么问题、它靠什么机制解决、它在哪些边界条件下容易出错。

本章反复出现的概念有：`Spring Cloud`、`Spring`、`IoC`、`AOP`、`JDBC`、`ORM`、`MVC`、`Spring Boot`、`Kafka`、`Redis`、`模块`、`WebSocket`、`Maven`、`依赖管理`、`POM`、`JDK`、`包`、`变量`、`JSON`、`事务`、`Map`、`枚举类`、`方法`、`线程`、`List`、`BigDecimal`、`序列化`、`Jackson`。

代码层面需要熟悉的关键词有：`default`、`import`、`equals`、`double`、`main`。

## 1. 本章总览

这一部分覆盖：24. Spring Cloud开发。

- 相关概念：`Spring Cloud`、`Spring`、`IoC`、`AOP`、`JDBC`、`ORM`、`MVC`、`Spring Boot`

## 2. 项目架构设计

这一部分覆盖：24.1. 项目架构设计。

它的核心是：对一个系统来说，建立一个简单可靠的模型，不但能大大简化系统的设计，而且能以较少的代码实现一个稳定运行的系统，最大限度地减少各种难以预测的错误。；对于证券交易系统来说，其输入是所有交易员发送的买卖订单。系统接收到订单后，内部经过定序，再由撮合引擎进行买卖撮合，最后对成交的订单进行清算，买卖双方交换Base和Quote资产，即完成了交易。

- 相关概念：`Spring Cloud`、`Kafka`、`Redis`、`模块`、`WebSocket`

**重点理解**

- 对一个系统来说，建立一个简单可靠的模型，不但能大大简化系统的设计，而且能以较少的代码实现一个稳定运行的系统，最大限度地减少各种难以预测的错误。
- 对于证券交易系统来说，其输入是所有交易员发送的买卖订单。系统接收到订单后，内部经过定序，再由撮合引擎进行买卖撮合，最后对成交的订单进行清算，买卖双方交换Base和Quote资产，即完成了交易。
- 在撮合成交的过程中，系统还需要根据成交价格、成交数量以及成交时间，对成交数据进行聚合，以便交易员能直观地以K线图的方式看到历史交易数据，因此，行情系统也是证券交易系统的一部分。此外，推送系统负责将行情、订单成交等事件推送给客户端。
- 最后，证券交易系统还需要给交易员提供一个操作界面，通常是Web或手机App。UI系统将在内部调用API，因此，API才是整个系统下单和撤单的唯一入口。
- 整个系统从逻辑上可以划分为如下模块：
- API模块（Trading API），交易员下单、撤单的API入口；

**复习抓手**

- 系统模块

## 3. 搭建项目框架

这一部分覆盖：24.2. 搭建项目框架。

它的核心是：我们以Spring Boot为基础，并通过Maven的模块化配置搭建了项目的基本结构，依赖的基础组件通过Docker Desktop运行并初始化数据。对于多个服务组成的分布式应用来说，使用Spring Cloud Config可满足应用的配置需求。

- 相关概念：`Spring Cloud`、`Maven`、`模块`、`依赖管理`、`POM`、`Spring Boot`、`JDK`、`Redis`、`Kafka`、`包`、`变量`、`JSON`
- 代码关键词：`default`、`import`

**重点理解**

- 我们以Spring Boot为基础，并通过Maven的模块化配置搭建了项目的基本结构，依赖的基础组件通过Docker Desktop运行并初始化数据。对于多个服务组成的分布式应用来说，使用Spring Cloud Config可满足应用的配置需求。

**复习抓手**

- 本地开发环境
- Spring Cloud Config
- 环境变量
- 常见错误
- 参考源码
- 小结

## 4. 24.3. 设计交易引擎

这一部分覆盖：24.3. 设计交易引擎、24.3.1. 设计资产系统、24.3.2. 设计订单系统、24.3.3. 设计撮合引擎、24.3.4. 设计清算系统、24.3.5. 完成交易引擎。

它的核心是：本节我们讨论并实现了一个基于内存的高性能的用户资产系统，其核心只有一个tryTransfer()转账方法，业务逻辑非常简单。；一个订单系统在内存中维护所有用户的活动订单，并提供删除和查询方法。

| 小节 | 要掌握什么 | 关键点 |
| --- | --- | --- |
| 000 设计交易引擎 | 理解本节的基本概念和示例 | `Spring Cloud`、`包`、`事务` |
| 24.3.1. 设计资产系统 | 本节我们讨论并实现了一个基于内存的高性能的用户资产系统，其核心只有一个tryTransfer()转账方法，业务逻辑非常简单。 | `Map`、`枚举类`、`方法`、`线程`、`JDK` |
| 24.3.2. 设计订单系统 | 一个订单系统在内存中维护所有用户的活动订单，并提供删除和查询方法。 | `包`、`Map`、`方法` |
| 24.3.3. 设计撮合引擎 | 本文讨论并实现了一个可工作的撮合引擎核心。实现撮合引擎的关键在于将业务模型转换为高效的数据结构。只要保证核心数据结构的简单和高效，撮合引擎的... | `List`、`方法`、`BigDecimal`、`包`、`equals` |
| 24.3.4. 设计清算系统 | 清算系统只负责根据撮合引擎输出的结果进行清算，清算的本质就是根据成交价格和数量对买卖双方的对应资产互相划转。清算系统本身没有状态。 | `方法` |
| 24.3.5. 完成交易引擎 | 交易引擎是以事件驱动的状态机模型，同样的输入将得到同样的输出。为提高交易系统的健壮性，可以自动检测重复消息和消息丢失并自动恢复。 | `模块`、`Kafka`、`Redis`、`线程`、`方法`、`包`、`序列化` |

**重点理解**

- 本节我们讨论并实现了一个基于内存的高性能的用户资产系统，其核心只有一个tryTransfer()转账方法，业务逻辑非常简单。
- 一个订单系统在内存中维护所有用户的活动订单，并提供删除和查询方法。
- 本文讨论并实现了一个可工作的撮合引擎核心。实现撮合引擎的关键在于将业务模型转换为高效的数据结构。只要保证核心数据结构的简单和高效，撮合引擎的业务逻辑编写是非常容易的。
- 清算系统只负责根据撮合引擎输出的结果进行清算，清算的本质就是根据成交价格和数量对买卖双方的对应资产互相划转。清算系统本身没有状态。
- 交易引擎是以事件驱动的状态机模型，同样的输入将得到同样的输出。为提高交易系统的健壮性，可以自动检测重复消息和消息丢失并自动恢复。

**复习抓手**

- 为什么不使用数据库？
- 为什么要使用ConcurrentMap？
- 如何扩展以支持更多的资产类型？
- 参考源码
- 小结
- Order的id和sequenceId为何不合并使用一个ID？
- 如何实现多个交易对？
- 如果有手续费，如何清算？
- 可以为挂单和吃单设置不同的手续费率吗？
- 可以设置负费率吗？
- 交易引擎崩溃后如何恢复？
- 如何序列化交易引擎的状态？

## 5. 设计定序系统

这一部分覆盖：24.4. 设计定序系统。

它的核心是：定序系统负责给每个事件一个唯一递增序列号。通过引用前一个事件的序列号，可以构造一个能自动检测连续性的事件流。

- 相关概念：`Spring Cloud`、`包`、`Spring`、`事务`

**重点理解**

- 定序系统负责给每个事件一个唯一递增序列号。通过引用前一个事件的序列号，可以构造一个能自动检测连续性的事件流。

**复习抓手**

- 如何在定序器重启后正确初始化下一个序列号？
- 如何在定序器崩溃后自动恢复？
- 如何解决定序的性能瓶颈？
- 参考源码
- 小结

## 6. 设计API系统

这一部分覆盖：24.5. 设计API系统。

它的核心是：API系统负责认证用户身份，并提供一个唯一的交易入口。

- 相关概念：`Spring Cloud`、`Filter`、`HTTP`、`JSON`、`String`、`序列化`、`Redis`、`Spring`、`包`、`Hmac`

**重点理解**

- API系统负责认证用户身份，并提供一个唯一的交易入口。

**复习抓手**

- 编写API Controller
- 如何实现API Key认证
- 内部系统调用API如何实现用户认证
- 如何跟踪API性能
- 参考源码
- 小结

## 7. 设计行情系统

这一部分覆盖：24.6. 设计行情系统。

它的核心是：行情系统是典型的少量写、大量读的模式，非常适合缓存。通过编写Lua脚本可使得更新Redis更加简单。

- 相关概念：`Spring Cloud`、`包`、`Redis`、`List`、`JSON`、`WebSocket`

**重点理解**

- 行情系统是典型的少量写、大量读的模式，非常适合缓存。通过编写Lua脚本可使得更新Redis更加简单。

**复习抓手**

- 参考源码
- 小结

## 8. 设计推送系统

这一部分覆盖：24.7. 设计推送系统。

它的核心是：要高效处理大量WebSocket连接，我们选择基于Netty的Vert.x框架，可以通过少量代码配合Redis实现推送。

- 相关概念：`Spring Cloud`、`包`、`WebSocket`、`JSON`、`Servlet`、`线程池`、`接口`、`Spring`、`模块`、`Spring Boot`、`方法`、`HTTP`

**重点理解**

- 要高效处理大量WebSocket连接，我们选择基于Netty的Vert.x框架，可以通过少量代码配合Redis实现推送。

**复习抓手**

- 参考源码
- 小结

## 9. 编写UI

这一部分覆盖：24.8. 编写UI。

它的核心是：UI系统是标准的Web系统，除了注册、登录外，主要交易功能均由页面JavaScript实现。UI系统本身不是交易入口，它通过转发JavaScript请求至真正的API入口。

- 相关概念：`Spring Cloud`、`MVC`
- 代码关键词：`main`

**重点理解**

- UI系统是标准的Web系统，除了注册、登录外，主要交易功能均由页面JavaScript实现。UI系统本身不是交易入口，它通过转发JavaScript请求至真正的API入口。

**复习抓手**

- 参考源码
- 小结

## 选择指南

| 遇到的问题 | 优先考虑 |
| --- | --- |
| 需要处理“本章总览”相关问题 | `Spring Cloud`、`Spring`、`IoC`、`AOP` |
| 需要处理“项目架构设计”相关问题 | `Spring Cloud`、`Kafka`、`Redis`、`模块` |
| 需要处理“搭建项目框架”相关问题 | `Spring Cloud`、`Maven`、`模块`、`依赖管理` |
| 需要处理“24.3. 设计交易引擎”相关问题 | `Spring Cloud`、`包`、`事务`、`Map` |
| 需要处理“设计定序系统”相关问题 | `Spring Cloud`、`包`、`Spring`、`事务` |
| 需要处理“设计API系统”相关问题 | `Spring Cloud`、`Filter`、`HTTP`、`JSON` |
| 需要处理“设计行情系统”相关问题 | `Spring Cloud`、`包`、`Redis`、`List` |
| 需要处理“设计推送系统”相关问题 | `Spring Cloud`、`包`、`WebSocket`、`JSON` |
| 需要处理“编写UI”相关问题 | `Spring Cloud`、`MVC`、`main` |

## 易错点清单

- 微服务拆分要围绕业务边界，不要简单按技术层拆。
- 交易系统最重要的是资产、订单、撮合、清算的一致性。
- API、行情、推送系统职责不同，性能目标也不同。
- 完整项目要同时关注可靠性、可观测性、部署和运维。

## 推荐复习路线

1. 先读“一句话总览”和“本章知识结构”，确认这一章在整个 Java 学习路线里的位置。
2. 按编号阅读各主题，看到表格时先抓“解决的问题”，再看“关键点”。
3. 回到源文件运行或重写关键示例，尤其是涉及 API、配置、异常、边界条件的部分。
4. 最后只看“易错点清单”，能解释每一条为什么会错，就说明这一章基本吃透。
