---
title: 3. 面向对象编程 - 复习对照
date: 2026-05-11
summary: 面向对象编程的目标，是把程序从“语句堆叠”升级成“对象协作”：用类表达数据和行为，用继承、多态、接口、包和核心类库组织更大的系统。
tags: [Java, 技术总结, 面向对象编程]
---

# 3. 面向对象编程 - 复习对照

> 按 `14. 多线程-总结.md` 的学习笔记方式重写：先给全局结构，再逐主题讲清楚问题、方法、适用场景和易错点。

## 一句话总览

面向对象编程的目标，是把程序从“语句堆叠”升级成“对象协作”：用类表达数据和行为，用继承、多态、接口、包和核心类库组织更大的系统。

## 本章知识结构

| 模块 | 解决的问题 | 核心关键词 |
| --- | --- | --- |
| 本章总览 | 理解并掌握 本章总览 的核心概念和用法 | `方法`、`包`、`继承`、`多态`、`classpath`、`JAR`、`String`、`包装类型` |
| 3.1. 面向对象基础 | 在OOP中，`class`和 instance是“模版”和“实例”的关系； | `方法`、`构造方法`、`方法重载`、`继承`、`多态`、`抽象类`、`接口`、`包` |
| 3.2. Java核心类 | 3.2.2. StringBuilder | `String`、`编码`、`StringBuilder`、`包装类型`、`JavaBean`、`枚举类`、`记录类`、`BigInteger` |

## 学习主线

这一章可以按“先理解概念，再看 API，再回到场景判断什么时候用”的顺序学习。不要只背方法名；每个知识点都要回答三个问题：它解决什么问题、它靠什么机制解决、它在哪些边界条件下容易出错。

本章反复出现的概念有：`方法`、`包`、`继承`、`多态`、`classpath`、`JAR`、`String`、`包装类型`、`JavaBean`、`构造方法`、`方法重载`、`抽象类`、`接口`、`作用域`、`内部类`、`变量`、`数组`、`基本类型`、`引用类型`、`编码`、`JVM`、`JDK`、`List`、`BigInteger`、`javac`、`Class类`、`字节码`、`Maven`。

代码层面需要熟悉的关键词有：`Object`、`package`、`class`、`new`、`int`、`public`、`double`、`private`、`null`、`return`、`void`、`this`、`String`、`extends`、`super`、`protected`、`final`、`instanceof`、`toString`、`equals`、`hashCode`、`abstract`、`interface`、`implements`、`default`、`static`、`main`、`import`。

## 1. 本章总览

这一部分覆盖：3. 面向对象编程。

- 相关概念：`方法`、`包`、`继承`、`多态`、`classpath`、`JAR`、`String`、`包装类型`、`JavaBean`
- 代码关键词：`Object`、`package`

## 2. 3.1. 面向对象基础

这一部分覆盖：3.1. 面向对象基础、3.1.1. 方法、3.1.2. 构造方法、3.1.3. 方法重载、3.1.4. 继承、3.1.5. 多态、3.1.6. 抽象类、3.1.7. 接口。

它的核心是：在OOP中，`class`和 instance是“模版”和“实例”的关系；；定义`class`就是定义了一种数据类型，对应的instance是这种数据类型的实例；

| 小节 | 要掌握什么 | 关键点 |
| --- | --- | --- |
| 000 面向对象基础 | 在OOP中，`class`和 instance是“模版”和“实例”的关系； | `方法`、`构造方法`、`方法重载`、`继承`、`多态`、`抽象类`、`接口` |
| 3.1.1. 方法 | 方法可以让外部代码安全地访问实例字段； | `方法`、`包`、`String`、`接口`、`变量`、`数组`、`基本类型` |
| 3.1.2. 构造方法 | 实例在创建时通过`new` 操作符会调用其对应的构造方法，构造方法用于初始化实例； | `构造方法`、`方法`、`引用类型`、`String`、`void`、`new`、`class` |
| 3.1.3. 方法重载 | 方法重载是指多个方法的方法名相同，但各自的参数不同； | `方法重载`、`方法`、`String`、`编码`、`int` |
| 3.1.4. 继承 | 继承是面向对象编程的一种强大的代码复用方式； | `继承`、`包`、`方法`、`构造方法`、`变量`、`引用类型`、`JVM` |
| 3.1.5. 多态 | 子类可以覆写父类的方法（Override），覆写在子类中改变了父类方法的行为； | `多态`、`继承`、`方法`、`变量`、`引用类型`、`String`、`构造方法` |
| 3.1.6. 抽象类 | 通过`abstract`定义的方法是抽象方法，它只有定义，没有实现。抽象方法定义了子类必须实现的接口规范； | `抽象类`、`多态`、`方法`、`包`、`继承`、`变量`、`接口` |
| 3.1.7. 接口 | Java的接口（`interface`）定义了纯抽象规范，一个类可以实现多个接口； | `接口`、`抽象类`、`方法`、`多态`、`继承`、`JDK`、`interface` |
| 3.1.8. 静态字段和静态方法 | 静态字段属于所有实例“共享”的字段，实际上是属于`class`的字段； | `方法`、`变量`、`接口`、`抽象类`、`class`、`static`、`this` |
| 3.1.9. 包 | Java内建的`package`机制是为了避免`class`命名冲突； | `包`、`接口`、`JDK`、`JVM`、`继承`、`作用域`、`方法` |
| 3.1.10. 作用域 | Java内建的访问权限包括`public`、 `protected`、 `private`和 `package`权限； | `作用域`、`接口`、`包`、`方法`、`继承`、`变量`、`public` |
| 3.1.11. 内部类 | Java的内部类可分为Inner Class、Anonymous Class和Static Nested Class三种； | `内部类`、`包`、`Class类`、`作用域`、`方法`、`接口`、`继承` |
| 3.1.12. classpath和jar | JVM通过环境变量classpath决定搜索`class`的路径和顺序； | `classpath`、`JAR`、`JVM`、`变量`、`字节码`、`方法`、`包` |
| 3.1.13. class版本 | 高版本的JDK可编译输出低版本兼容的`class`文件，但需注意，低版本的JDK可能不存在高版本JDK添加的类和方法，导致运行时报错。 | `JDK`、`JVM`、`javac`、`String`、`方法`、`变量`、`Lambda` |
| 3.1.14. 模块 | Java 9引入的模块目的是为了管理依赖； | `模块`、`JDK`、`JVM`、`Class类`、`JAR`、`classpath`、`继承` |
| Overload | Overload（重载）是指**在同一个类中，定义多个方法名相同但参数列表不同的方法**。 | 正文示例、使用场景 |
| Override | Override（覆写）是面向对象编程中的一个重要概念，指的是**子类重新实现父类中已有的方法**。 | 正文示例、使用场景 |
| this | ================================================================== 解释 | `name`、`"小明"`、`this.name`、`p`、`this` |

**重点理解**

- 在OOP中，`class`和 instance是“模版”和“实例”的关系；
- 定义`class`就是定义了一种数据类型，对应的instance是这种数据类型的实例；
- `class`定义的field，在每个instance都会拥有各自的field，且互不干扰；
- 通过`new` 操作符创建新的instance，然后用变量指向它，即可通过变量来引用这个instance；
- 访问实例字段的方法是变量名 . 字段名；
- 指向instance的变量都是引用变量。
- 方法可以让外部代码安全地访问实例字段；
- 方法是一组执行语句，并且可以执行任意逻辑；
- 方法内部遇到`return`时返回，`void`表示不返回任何值（注意和返回`null`不同）；
- 外部代码通过`public`方法操作实例，内部代码可以调用`private`方法；
- 理解方法的参数绑定。
- 实例在创建时通过`new` 操作符会调用其对应的构造方法，构造方法用于初始化实例；

**复习抓手**

- class和instance
- 定义class
- 创建实例
- 小结
- 定义方法
- private方法
- this变量
- 方法参数
- 可变参数
- 参数绑定
- 默认构造方法
- 多个构造方法

## 3. 3.2. Java核心类

这一部分覆盖：3.2. Java核心类、3.2.1. 字符串和编码、3.2.2. StringBuilder、3.2.3. StringJoiner、3.2.4. 包装类型、3.2.5. JavaBean、3.2.6. 枚举类、3.2.7. 记录类。

它的核心是：3.2.2. StringBuilder；3.2.3. StringJoiner

| 小节 | 要掌握什么 | 关键点 |
| --- | --- | --- |
| 000 Java核心类 | 3.2.2. StringBuilder | `String`、`编码`、`StringBuilder`、`包装类型`、`JavaBean`、`枚举类`、`记录类` |
| 3.2.1. 字符串和编码 | Java字符串String是不可变对象； | `String`、`编码`、`引用类型`、`数组`、`方法`、`接口`、`包` |
| 3.2.2. StringBuilder | StringBuilder是可变对象，用来高效拼接字符串； | `StringBuilder`、`String`、`方法`、`编码`、`数组`、`线程`、`this` |
| 3.2.3. StringJoiner | 用指定分隔符拼接字符串数组时，使用StringJoiner或者`String`.join()更方便； | `String`、`StringBuilder`、`数组`、`方法` |
| 3.2.4. 包装类型 | Java核心库提供的包装类型可以把基本类型包装为`class`； | `包装类型`、`基本类型`、`引用类型`、`接口`、`包`、`Class类`、`NullPointerException` |
| 3.2.5. JavaBean | JavaBean是一种符合命名规范的`class`，它通过getter和 setter来定义属性； | `JavaBean`、`方法`、`String`、`class`、`private`、`public`、`boolean` |
| 3.2.6. 枚举类 | Java使用`enum`定义枚举类型，它被编译器编译为`final` `class` Xxx `extends` Enum { … }； | `枚举类`、`String`、`包`、`变量`、`引用类型`、`方法`、`JVM` |
| 3.2.7. 记录类 | 从Java 14开始，提供新的`record`关键字，可以非常方便地定义Data Class： | `记录类`、`String`、`变量`、`方法`、`构造方法`、`枚举类`、`Class类` |
| 3.2.8. BigInteger | BigInteger用于表示任意大小的整数； | `BigInteger`、`数组`、`方法`、`继承`、`基本类型`、`long`、`int` |
| 3.2.9. BigDecimal | BigDecimal用于表示精确的小数，常用于财务计算； | `BigDecimal`、`BigInteger`、`方法`、`数组`、`包`、`equals` |
| 3.2.10. 常用工具类 | Java提供的常用工具类有： | `方法`、`数组`、`String`、`JDK`、`byte` |

**重点理解**

- 3.2.2. StringBuilder
- 3.2.3. StringJoiner
- Java字符串String是不可变对象；
- 字符串操作不改变原字符串内容，而是返回新字符串；
- 常用的字符串操作：提取子串、查找、替换、大小写转换等；
- Java使用Unicode编码表示String和 `char`；
- 转换编码就是将String和 `byte`[]转换，需要指定编码；
- 转换为`byte`[]时，始终优先考虑UTF-8编码。
- StringBuilder是可变对象，用来高效拼接字符串；
- StringBuilder可以支持链式操作，实现链式操作的关键是返回实例本身；
- StringBuffer是 StringBuilder的线程安全版本，现在很少使用。
- 用指定分隔符拼接字符串数组时，使用StringJoiner或者`String`.join()更方便；

**复习抓手**

- String
- 字符串比较
- 去除首尾空白字符
- 替换子串
- 分割字符串
- 拼接字符串
- 格式化字符串
- 类型转换
- 转换为char[]
- 字符编码
- 延伸阅读
- 小结

## 选择指南

| 遇到的问题 | 优先考虑 |
| --- | --- |
| 需要处理“本章总览”相关问题 | `方法`、`包`、`继承`、`多态` |
| 需要处理“3.1. 面向对象基础”相关问题 | `方法`、`构造方法`、`方法重载`、`继承` |
| 需要处理“3.2. Java核心类”相关问题 | `String`、`编码`、`StringBuilder`、`包装类型` |

## 易错点清单

- 字段应尽量私有，通过方法维护对象状态，不要把内部数据随便暴露出去。
- 重载看参数列表，覆写看父子类同签名方法，二者不要混淆。
- 多态调用看运行期真实类型，不是变量声明类型。
- 抽象类适合抽取共同状态和模板，接口适合表达能力和规范。
- `classpath`、`jar`、模块影响程序能否找到类，不只是打包细节。

## 推荐复习路线

1. 先读“一句话总览”和“本章知识结构”，确认这一章在整个 Java 学习路线里的位置。
2. 按编号阅读各主题，看到表格时先抓“解决的问题”，再看“关键点”。
3. 回到源文件运行或重写关键示例，尤其是涉及 API、配置、异常、边界条件的部分。
4. 最后只看“易错点清单”，能解释每一条为什么会错，就说明这一章基本吃透。
